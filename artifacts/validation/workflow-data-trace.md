# Workflow Data Trace

Status: **PASS**
Workflows: 9 · Manual-ready: 9 · Scheduled: 3 · Push-triggered: 2

| Workflow | Lane | Triggers | Consumes | Produces | Failure boundary |
|---|---|---|---|---|---|
| ci-health-recovery.yml | ci-health | workflow_run:Validate Repo<br>workflow_dispatch | Validate Repo exact-SHA workflow conclusion | automation health receipt<br>governed GitHub issue alert/recovery state | A RED exact-SHA result is recorded and surfaced; no success state is inferred from a failed or unobserved run. |
| daily-citation-intelligence.yml | signal-intelligence | schedule:17-13-utc<br>workflow_dispatch | traffic-qualified fixtures<br>source registry<br>strategy contract | data/signals/**<br>artifacts/validation/**<br>reports/** | Signal intelligence may update generated state but must not commit governance or source authority files. |
| deploy-distribution.yml | deploy | workflow_run:Validate Repo<br>workflow_dispatch | velocity-validated-{commit_sha}<br>artifacts/release/validation-attestation.json<br>.build/indexnow-priority.txt<br>.build/indexnow-batch.txt | distribution evidence | The exact artifact commit SHA must match the validation attestation before distribution. |
| query-class-occupancy-probe.yml | measurement | workflow_dispatch | data/signals/query_class_probe_panel.json<br>data/authority_scale/query_atlas.json (measured T1 head, via --atlas-top)<br>OpenRouter web-plugin citation annotations | data/signals/query_class_occupancy.json<br>data/authority_scale/query_atlas.json<br>data/queries/measured_demand_candidates.json | Dispatch-only, so it is not a scheduled mutator and does not consume the single scheduled-commit slot; the scheduled path runs the same probe inside query-evidence-refresh.yml. It commits its measurement instead of uploading an artifact, because the previous version left nothing behind for the atlas to read. It measures answer-engine citation slots via the OpenRouter web plugin, not Bing SERP slots: Bing blocks both residential and GitHub Actions egress with a JavaScript shell, which is why the only run of the previous version discarded 16 of 16 probes including both controls and still exited 0. A degraded probe is discarded, never recorded as zero occupancy, and a run that measures nothing or loses its controls exits non-zero. |
| query-evidence-refresh.yml | query-evidence | schedule:23-06-utc<br>workflow_dispatch | Google Search Console search-analytics query rows for the verified property<br>existing evidence_queries.json (merged, never replaced)<br>OpenRouter web-plugin citation annotations for the occupancy panel and the measured atlas head | data/queries/evidence/evidence_queries.json<br>data/authority_scale/query_atlas.json<br>data/signals/query_class_occupancy.json<br>data/queries/measured_demand_candidates.json<br>Velocity Content Release run when measured demand produced candidates | Ingests measured Search Console demand, measures answer-engine citation occupancy, rebuilds the atlas on that measured winnability signal, joins it to the release queue as page candidates, and commits only if the full validation chain passes in the same run. Named in allowed_scheduled_mutation_workflows. It has no page-admission authority: the join proposes candidates and the release law downstream still decides. A push made with GITHUB_TOKEN raises no push event, so when the join produces candidates this workflow dispatches Velocity Content Release explicitly; when it produces none it dispatches nothing and prints the named stop reason. Missing credentials leave the evidence untouched and exit 0; a credentialed API failure is a hard error. A failed occupancy probe leaves the previously committed reading in place - stale, never a false zero. |
| search-intelligence-loop.yml | search-intelligence | schedule:41-14-utc<br>workflow_dispatch | admitted route registry<br>read-only Agent Run normalization<br>optional provider truth evidence<br>source/claim registries | data/search_intelligence/**<br>Search Intelligence validation receipts | Scheduled lane observes, diagnoses, queues, retests, and reports only. It has no independent page-admission, cadence, or repo-commit authority. |
| validate-repo.yml | validate | push:main<br>pull_request<br>workflow_dispatch | repository source<br>package-lock.json<br>validation registry | velocity-validated-{commit_sha}<br>validation diagnostics | No artifact is uploaded as validated unless the staged container release passes. |
| velocity-content-release.yml | content-release | push:agent_run_manifest<br>push:measured_demand_candidates<br>workflow_dispatch | Twin Agent ready manifests<br>traffic-qualified preview proof<br>staged Velocity source<br>data/queries/measured_demand_candidates.json (measured query demand ranked by citation occupancy) | absorbed agent run commit<br>validated Velocity release commit<br>release diagnostics<br>data/queries/evidence/evidence_queries.json<br>data/authority_scale/query_atlas.json<br>Validate Repo run on the pushed main HEAD | Absorption pushes on its own commit once the release gate passes on the normalized artifacts alone, so a content-release failure cannot un-absorb a landed agent run. The content release then pushes only after Safe Harbor/transactional release finalization, deterministic rebuild, release validation, exact-agent validation, and citation-fix validation pass. A push made with GITHUB_TOKEN raises no push event, so after any commit this workflow lands it dispatches Validate Repo on main: the absorption and release commits are validated by the same unmodified guards that govern them, and a repair that did not hold shows up red instead of leaving main pinned to a stale result. |
| velocity-full-rebuild.yml | full-rebuild | workflow_dispatch | repository source<br>citation strategy gate<br>frozen accepted-output registry | validated rebuild commit<br>rebuild diagnostics | Push only after deterministic frozen-aware rebuild and release validation pass; broad self-healing is not permitted in the rebuild workflow. |
