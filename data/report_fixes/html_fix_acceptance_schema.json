{
  "schema_version": "1.0",
  "required_top_level_keys": ["schema_version", "status", "generated_by", "entries"],
  "entry_required_keys": ["implementation_path", "artifacts", "row_requirements", "required_strings", "required_artifact_types"],
  "row_requirement_required_keys": ["row_id", "query", "source_fix", "required_blocks"],
  "block_required_keys": ["type", "heading_exact", "min_rows", "placement"],
  "production_rule": "Generated from source HTML/PDF artifact rows. Hand-authored vertical-specific manifests are allowed only as incident fixtures or regression fixtures."
}
