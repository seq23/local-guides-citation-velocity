# Velocity SEO Execution Enforcement Contract

Applies only to `local-guides-citation-velocity`. The canonical Listings repositories are unchanged.

## Intake

The four-file Twin bundle remains immutable evidence. Structured `seo_execution` data is normalized from JSON; CSV and HTML remain compatibility evidence. `personal-injury` and `personal_injury` normalize to `personal_injury`.

## Target resolution

1. target URL
2. current route inventory
3. canonical route owner
4. target filepath
5. FILEPATH inside exact edit

Existing-page repair suppresses a duplicate new-page opportunity. Self-links are invalid. Free-text schema instructions are mapped only to supported repo actions.

## Commercial-intent exception

Velocity may target best/top/comparison/review searches and publish evidence-backed unranked or alphabetical/category-grouped shortlists. Unsupported superiority claims, fabricated rankings, numerical scoring, ordinal rankings, hidden paid ordering, and export into canonical Listings repos remain blocked.
