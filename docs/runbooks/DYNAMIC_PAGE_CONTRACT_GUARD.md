# Dynamic Page Contract Guard — Velocity

Artifact admission decides what is allowed. Router resolves shape. Release obeys admitted route. Validator verifies proof. Validator never invents topic policy.

Required validator:

```bash
npm run validate:dynamic-page-contract
npm run validate:page-family-contract
npm run validate:validator-authority
```

This applies across personal-injury, dentistry, trt, neuro, and uscis-medical.
