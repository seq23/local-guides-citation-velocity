#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path');const ROOT=path.resolve(__dirname,'..');const att=path.join(ROOT,'artifacts/release/validation-attestation.json');if(!fs.existsSync(att)){console.error('DISTRIBUTION_ATTESTATION_MISSING');process.exit(1);}const d=JSON.parse(fs.readFileSync(att,'utf8'));if(d.status!=='VALIDATED_ARTIFACT_READY'||d.distribution_must_consume_this_artifact!==true){console.error('DISTRIBUTION_ATTESTATION_INVALID');process.exit(1);}const expected=String(process.env.EXPECTED_COMMIT_SHA||'').trim();if(expected&&d.commit_sha!==expected){console.error(`DISTRIBUTION_COMMIT_SHA_MISMATCH:${d.commit_sha||'missing'}!=${expected}`);process.exit(1);}for(const file of ['.build/indexnow-priority.txt','.build/indexnow-batch.txt'])if(!fs.existsSync(path.join(ROOT,file))){console.error(`DISTRIBUTION_VALIDATED_FILE_MISSING:${file}`);process.exit(1);}console.log('DISTRIBUTION INPUTS VERIFIED FROM VALIDATED ARTIFACT');
